project Subject is

   for Languages use ("Asm");
   for Source_Dirs use ("stubs");
   for Object_Dir use "obj";

   Ada_Linker_Switches :=
     ("-T", "../subject.ld",
      "-nostdlib",
      "-n");

   package Naming is
      for Body_Suffix ("Asm") use ".S";
   end Naming;

end Subject;
