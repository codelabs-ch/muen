library project Liblog is

   for Languages use ("Ada");
   for Source_Dirs use ("liblog");
   for Object_Dir use "../obj/liblog";
   for Library_Dir use "../obj/lib";
   for Library_Name use "log";
   for Library_Kind use "static";

   package Compiler is
      for Default_Switches ("Ada") use ("-fcallgraph-info=su", "-gnatp", "-gnatws");
   end Compiler;

end Liblog;
