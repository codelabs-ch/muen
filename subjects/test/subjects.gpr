project Subjects extends "../subject" is

   for Languages use ("Ada");
   for Source_Dirs use ("../../common/src", "../../common/debug", "src");
   for Object_Dir use "obj";

   for Main use ("subject_a", "subject_b");

end Subjects;
